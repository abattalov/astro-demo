---
import '../styles/global.css'
import '../components/Navigation.astro'
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "About Me";

const identity = {
    firstName: "Alisher",
    country: "United States",
    occupation: "Software Engineer",
    hobbies: ["disc golf", "programming", "Jiu-Jitsu"]
}

const skills = ["HTML", "CSS", "JavaScript", "TypeScript", "React", "Astro"]

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "navy";
const fontWeight = "bold";
const textCase = "uppercase";
---

<BaseLayout pageTitle={pageTitle}>
    <style define:vars={{skillColor, fontWeight, textCase}}>
        h1 {
            color: purple;
            font-size: 4rem;
        }

        .skill {
            color: var(--skillColor);
            font-weight: var(--fontWeight);
            text-transform: var(--textCase);
        }
    </style>

    <h1>{pageTitle}</h1>
    <h2>... and my new Astro site!</h2>
    
    <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Fugit eaque similique ex molestias error est, voluptas maiores esse ea sunt suscipit vel, necessitatibus, quis aliquam tenetur. Magni explicabo velit facere?</p>
    
    <p>Lorem ipsum dolor sit, amet consectetur adipisicing elit. Temporibus, inventore nam. Odit possimus harum, maxime perspiciatis itaque obcaecati sunt voluptates eum debitis odio impedit sed et accusamus assumenda neque provident!</p>
    
    <p>Here are a few facts about me:</p>
    <ul>
        <li>My name is {identity.firstName}.</li>
        <li>I live in {identity.country} and I work as a {identity.occupation}.</li>
        {identity.hobbies.length >= 2 &&
            <li>Two of my hobbies are: {identity.hobbies[0]} and {identity.hobbies[2]}</li>
        }
    </ul>
    <p>My skills are:</p>
    <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
    </ul>
    {happy && <p>I am happy to be learning Astro!</p>}
    
    {finished && <p>I finished this tutorial!</p>}
    
    {goal === 3 ? <p>My goal is to finish in 3 days.</p> : <p>My goal is not 3 days.</p>}
</BaseLayout>

  
